{
  "name": "terraform-provider-hrobot-hooks",
  "private": true,
  "description": "git-hook tooling for terraform-provider-hrobot (husky only — not a node package)",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "packageManager": "pnpm@9.15.4"
}
